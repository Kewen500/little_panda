# Assets

- `api-balance-widget.png`: README preview image.
- `api-balance-widget.ico`: Windows application and shortcut icon.
- `create_icon.ps1`: Regenerates the PNG and ICO icon files using Windows drawing APIs.
- `panda.png`: Optional custom mascot image. When absent, the widget draws its built-in panda.
